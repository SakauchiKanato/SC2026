# AGENTS.md

AIを使った開発における、チーム共通のエージェントルール。
全員が同じ基準で作業できることを目的とする。

---

## 1. 動作の基本原則

- 指示されたことだけを実行する（関係のないファイルは変更しない）
- 大きな変更の前には必ず確認を取る
- 情報が不足している場合は推測で進めず、質問する
- 1タスク＝1変更を基本とし、変更範囲を小さく保つ

---

## 2. 実装前の確認・計画（最重要）

### 基本方針
- **実装に入る前に必ずユーザーの意図を確認する**
- 曖昧な点が1つでもある場合は、作業を止めて質問する
- 「たぶんこういう意味だろう」という推測で進めない

### Planモードの活用
- 可能なツール（Claude Code など）では **Planモードを使って実装計画を先に提示する**
- 計画の内容をユーザーが承認してから実装を開始する
- 計画に含める内容：
  - 変更するファイルと理由
  - 実装のアプローチ（複数ある場合は選択肢を提示）
  - 影響が出る可能性のある範囲

### 確認すべき場面
- 要件が抽象的・曖昧なとき
- 複数の実装方法が考えられるとき
- 変更範囲が広くなりそうなとき
- 既存の設計・構造に影響が出るとき
- 削除・リネームなど元に戻しにくい操作をするとき

---

## 3. コードの品質

- 既存のコードスタイル・命名規則に合わせる
- コメントは「何をしているか」ではなく「なぜそうするか」を書く
- マジックナンバーは使わず、定数として定義する
- エラーは握りつぶさず、適切に処理する
- 新しいライブラリは勝手に追加しない
- 変数名・関数名は省略せず、役割が一目でわかる名前にする（例：`res`ではなく`search_result`）

---

## 4. セキュリティの基本原則（AIが軽視しやすいため必須）

AIエージェントはスピード優先でコードを書く際、セキュリティ上の問題を無意識に生成しやすい。
以下は**例外なく守る**こととする。

- **パスワードは平文で保存・比較しない**：必ず`bcrypt`等でハッシュ化する
- **機密情報をログに出力しない**：`print()`でメールアドレス・トークン・APIキー・URLなどを出力しない。`logging`モジュールを使い、必要ならマスキングする
- **CORSをワイルドカードのまま実装しない**：`allow_origins=["*"]`を本番想定のコードに書かない。許可ドメインは環境変数で管理する
- **クライアントから受け取ったIDをそのまま信用しない**：`user_id`等はリクエストボディやURLパラメータではなく、認証トークン（JWT等）から取得する
- **フロントのバリデーションだけで済ませない（Zero Trust）**：バックエンド側でも必ず入力値を検証する
- **APIキー・シークレットをコードに直書きしない**：`.env`で管理し、`.gitignore`に必ず含める
  - 誤ってpush・公開してしまった場合は、**最優先でキーを無効化**してから履歴削除などの対応を行う（無効化前に`git reset`等で済ませようとしない）
- セキュリティ上、本来は直すべきだが時間の都合で後回しにする場合は、口頭やSlackでの共有だけで済ませず、`TODO(SECURITY):`コメント＋Issue化を必須とする

---

## 5. アーキテクチャ・設計の基本原則

- **日付・金額を文字列型で保存しない**：`Date`/`DateTime`/`Numeric`など適切な型を使う（ソート・計算・比較の不具合を防ぐ）
- **N+1問題を避ける**：ループ内で毎回クエリを実行しない。`join`や`joinedload`等でまとめて取得する
- **データ整合性が必要な複数の更新処理はトランザクションでまとめる**：途中でエラーが起きた場合は`rollback`し、中途半端な状態を残さない
- **HTTPステータスコードを正しく使い分ける**：成功・失敗をすべて200で返さない（作成=201、削除=204、認証エラー=401、権限エラー=403、リソースなし=404 など）
- **RESTfulなURL設計にする**：`/api/get_users`のような動詞入りURLを避け、リソース名＋HTTPメソッドで表現する

---

## 6. 依存関係・環境変数の管理

- **依存パッケージのバージョンを固定する**：`requirements.txt`や`package.json`で`^`や無指定のままにせず、動作確認済みのバージョンに固定する
- **環境変数は起動時にバリデーションする**：値が未設定のまま起動してしまう実装にしない（Pydantic Settings等の活用を推奨）
- **`.env.example`を用意する**：必要な環境変数の一覧をリポジトリに含め、READMEにセットアップ手順を書く

---

## 7. コード量・ファイルサイズ

- 1ファイルは300行以内を目安にする（超える場合は分割を提案する）
- 関数は1つの役割だけを持たせる
- 同じ処理はコピペせず共通化する
- シンプルに書ける方法があればそちらを優先する
- 使われていないコード・変数は残さない
- 一時的な検証用ファイル（`try.jsx`, `xxx_old.py`など）は作業完了後に削除する
- ライブラリで実現できることを自前で実装しない
- **優先順位：読みやすさ ＞ 短さ**（行数削減のために読みにくくしない）

---

## 8. テスト

- ロジックを追加・変更したときは必ずテストを書く
- テストなしのPRはマージしない
- テストは実装と同じPRに含める
- 特に認証周り・コア機能・外部API連携部分は優先してテストを書く

---

## 9. 運用・監視

- 障害検知のため、`/health`のようなヘルスチェック用エンドポイントを用意する
- ログレベルは環境変数で切り替えられるようにする（開発=DEBUG、本番=WARNING等）

---

## 10. チーム開発のルール

- コミットメッセージは以下のフォーマットを使う
  - `feat:` 新機能
  - `fix:` バグ修正
  - `chore:` 設定・ツール変更
  - `refactor:` リファクタリング
  - `docs:` ドキュメント変更
- PRの説明には「何を・なぜ・どう変えたか」を書く
- ロジックを変更するときはテストも合わせて変更する
- `.env` やシークレット情報は絶対にコードに含めない
- 他のメンバーが触っているファイルには慎重に対応する

---

## 11. AIの暴走・ミスを防ぐ

- 削除・リネームは元に戻せないため、必ず確認してから実行する
- 自動生成したコードはレビューしてからマージする
- 方針の選択肢が複数ある場合は提示して、人間が決める
- `TODO` や `FIXME` は勝手に削除しない
- 想定外の挙動を発見した場合は作業を止めて報告する

---

## 12. ドキュメント・引き継ぎ

- コードを変更したら関連するREADMEやコメントも更新する
- 複雑な処理には「なぜこうしたか」の背景を残す
- 新しいメンバーが読んでも理解できるレベルで書く
